<div class="container">
  <h1 class="mb-4 pt-4">Create a new bookmark</h1>
  <hr>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.association :list, selected: @list[:id] %>
    <%= f.submit %>
  <% end %>

</div>
